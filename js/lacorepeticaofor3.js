for (let horas = 0; horas < 24; horas = horas + 1){
    for (let minutos = 0; minutos < 60; minutos = minutos + 1){
        for (let segundos = 0; segundos < 60; segundos = segundos + 1){
            console.log(`${horas} : ${minutos} : ${segundos}`)
        }
    }
}